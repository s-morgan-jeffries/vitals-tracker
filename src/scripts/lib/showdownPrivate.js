define([
  'showdown'
], function (Showdown) {
  'use strict';

  //// For the sake of parity, calling this `root`
  //var root = window;
  //
  //// Now set root.Showdown to undefined
  //root.Showdown = void 0;

  // And return Showdown
  return Showdown;
});
